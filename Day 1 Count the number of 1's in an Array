// Day 1 (Count the number of 1's in an array)

#include<iostream>
#include <vector>
using namespace std;
int main(){
    int count = 0;
    int arr[7] = {0,0,0,0,1,1,1};
    for(int i=0;i<7;i++){
        if(arr[i]==1){
            count++;
        }
    }
    cout<<count;
    return 0;
}
